{
  "buildCommand": "cd ../.. && pnpm install && pnpm build:marketing",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
